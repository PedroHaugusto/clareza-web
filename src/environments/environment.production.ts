export const environment = {
  producao: true,
  // Lembre de adicionar a origem publicada em CORS_ALLOWED_ORIGINS no painel do Render,
  // senao o navegador bloqueia as chamadas mesmo com a API no ar.
  urlDaApi: 'https://clareza-api.onrender.com',
};
